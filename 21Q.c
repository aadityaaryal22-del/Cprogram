//Write a program to display the size of int.
#include <stdio.h>
#include <conio.h>

int main() {
    int num;

    printf("Size of int is: %zu megabytes", sizeof(num));

    getch();
    return 0;
}
