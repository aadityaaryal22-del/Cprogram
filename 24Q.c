// Write a program to display the size of `character`.
#include <stdio.h>
#include <conio.h>

int main() {
    char name;

    printf("Size of char is: %zu bytes", sizeof(name));

    getch();
    return 0;
}
