// Write a program to display the size of `float`.
#include <stdio.h>
#include <conio.h>

int main() {
    float num;

    printf("Size of float is: %zu bytes", sizeof(num));

    getch();
    return 0;
}
