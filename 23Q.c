// Write a program to display the size of `double`.
#include <stdio.h>
#include <conio.h>

int main() {
    double num;

    printf("Size of double is: %zu bytes", sizeof(num));

    getch();
    return 0;
}
